# 通达信数据路径（必填）
TDX_PATH=C:\new_tdx

# 数据库配置
# 支持postgresql, mysql, sqlite
DB_TYPE=postgresql
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tdx_data
DB_USER=postgres
DB_PASSWORD=postgres
DB_BATCH_SIZE=1000

# 输出CSV路径
CSV_OUTPUT_PATH=./output

# 日志级别
LOG_LEVEL=INFO

# 处理选项
USE_TQDM=True  # 是否显示进度条
